import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog",
};

const Blog = () => {
  return <h1>Blog Page Nested route</h1>;
};

export default Blog;
